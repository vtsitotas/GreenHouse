gradle-wrapper.jar
/.gradle
/captures/
/gradlew
/gradlew.bat
/local.properties
GeneratedPluginRegistrant.java
.cxx/

# Remember to never publicly share your keystore.
# See https://flutter.dev/to/reference-keystore
key.properties
**/*.keystore
**/*.jks

# Firebase config/credentials — google-services.json isn't a hard secret
# (its API key is package/SHA-restricted) but doesn't need to be in git;
# the admin SDK key is a real credential and must never be committed.
app/google-services.json
app/*firebase-adminsdk*.json
